{% extends "base.twig" %}

{% block content %}
	{% if post %}
		<h1>{{post.post_name}}</h1>
		{{post.post_content}}
	{% endif %}

	<div class="row">
		{% for post in posts %}
			{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
		{% endfor %}
	</div>
{% endblock %}